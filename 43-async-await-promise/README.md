# Async Await with Promise based Errors

* open api/project-routes.js
* use async await to fetch data from /projects route
* use try catch to handle errors
* use custom functions such `throwIf` and `throwError`.
